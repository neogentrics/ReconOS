# Build output
build/
# The sanitizers and the static analyzer each want their own tree: both change
# the compiler flags, and sharing a directory with the ordinary build means
# every switch between them is a full rebuild.
build-sanitized/
build-analyzed/
out/
*.o
*.a
*.so
ReconOS

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
compile_commands.json
.cmake/

# Generated Wayland protocol code
xdg-shell-protocol.c
xdg-shell-protocol.h

# Editors and IDEs
.vs/
.vscode/
.idea/
*.swp
*~

# OS cruft
.DS_Store
Thumbs.db

# Logs
*.log
dist/
kernel/build/disk.img
kernel/build/fixtures/
